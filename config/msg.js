const msg = {
	hello: "Welcome to the httpcats bot! Type a HTTP status code to receive a correspondent cat.",
	wrongRoute : "Invalid command. Try sending a HTTP status like 404 or 500",
	badRequest : "Sorry, this HTTP status doesn´t exist or the server is down"
}

module.exports = msg;